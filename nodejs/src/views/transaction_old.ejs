<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transaction Details - Secure Banking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .transaction-amount.credit { color: #28a745; }
        .transaction-amount.debit { color: #dc3545; }
        .sidebar { background-color: #f8f9fa; border-radius: 0.375rem; }
        .breadcrumb { background-color: transparent; padding: 0; margin-bottom: 1rem; }
        .breadcrumb-item a { text-decoration: none; }
        .related-transaction { 
            padding: 0.5rem; 
            border-bottom: 1px solid #dee2e6; 
            transition: background-color 0.2s;
        }
        .related-transaction:hover { background-color: #f8f9fa; }
        .related-transaction:last-child { border-bottom: none; }
        .note-content { 
            background-color: #f8f9fa; 
            border-radius: 0.375rem; 
            padding: 1rem;
            border: 1px solid #dee2e6;
        }
        .note-content.has-content { 
            background-color: #fff3cd; 
            border-color: #ffeaa7;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/dashboard">
                <i class="fas fa-university me-2"></i>Secure Banking
            </a>
            <div class="navbar-nav ms-auto">
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user me-1"></i><%= user.firstName %> <%= user.lastName %>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="/profile"><i class="fas fa-user-cog me-2"></i>Profile</a></li>
                        <li><a class="dropdown-item" href="/preferences"><i class="fas fa-cog me-2"></i>Preferences</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Logout</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <!-- Breadcrumb -->
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/dashboard"><i class="fas fa-home"></i> Dashboard</a></li>
                <li class="breadcrumb-item"><a href="/transactions/search">Transactions</a></li>
                <li class="breadcrumb-item active" aria-current="page">Transaction Details</li>
            </ol>
        </nav>

        <div class="row">
            <!-- Main Content -->
            <div class="col-lg-8">
                <!-- Transaction Details Card -->
                <div class="card mb-4">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">
                            <i class="fas fa-receipt me-2"></i>Transaction Details
                        </h5>
                        <button class="btn btn-outline-primary btn-sm" onclick="window.print()">
                            <i class="fas fa-print me-1"></i>Print
                        </button>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <h6 class="text-muted mb-2">Transaction Information</h6>
                                <table class="table table-borderless">
                                    <tr>
                                        <td class="fw-bold">Transaction ID:</td>
                                        <td>#<%= transaction.id %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold">Date:</td>
                                        <td><%= new Date(transaction.date).toLocaleDateString('en-US', { 
                                            year: 'numeric', 
                                            month: 'long', 
                                            day: 'numeric',
                                            hour: '2-digit',
                                            minute: '2-digit'
                                        }) %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold">Type:</td>
                                        <td>
                                            <span class="badge bg-<%= transaction.transactionType === 'credit' ? 'success' : 'danger' %>">
                                                <%= transaction.transactionType.toUpperCase() %>
                                            </span>
                                        </td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold">Amount:</td>
                                        <td>
                                            <span class="transaction-amount <%= transaction.transactionType %> fs-5 fw-bold">
                                                <%= transaction.transactionType === 'credit' ? '+' : '-' %>$<%= Math.abs(transaction.amount).toFixed(2) %>
                                            </span>
                                        </td>
                                    </tr>
                                </table>
                            </div>
                            <div class="col-md-6">
                                <h6 class="text-muted mb-2">Transaction Details</h6>
                                <table class="table table-borderless">
                                    <tr>
                                        <td class="fw-bold">Company:</td>
                                        <td><%= transaction.company %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold">Description:</td>
                                        <td><%= transaction.description %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold">Category:</td>
                                        <td>
                                            <span class="badge bg-secondary"><%= transaction.category %></span>
                                        </td>
                                    </tr>
                                    <% if (user.accountNumber) { %>
                                    <tr>
                                        <td class="fw-bold">Account:</td>
                                        <td>***<%= user.accountNumber.slice(-4) %></td>
                                    </tr>
                                    <% } %>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Transaction Note Card -->
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h6 class="mb-0">
                            <i class="fas fa-sticky-note me-2"></i>Transaction Note
                        </h6>
                        <button class="btn btn-outline-primary btn-sm" data-bs-toggle="modal" data-bs-target="#noteModal">
                            <i class="fas fa-edit me-1"></i>Edit Note
                        </button>
                    </div>
                    <div class="card-body">
                        <div class="note-content <%= renderedNote ? 'has-content' : '' %>">
                            <% if (renderedNote) { %>
                                <!-- VULNERABLE: Template injection vulnerability - user input rendered as EJS -->
                                <%- renderedNote %>
                            <% } else { %>
                                <em class="text-muted">No note added for this transaction.</em>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Sidebar -->
            <div class="col-lg-4">
                <!-- Account Summary -->
                <div class="card sidebar mb-4">
                    <div class="card-header">
                        <h6 class="mb-0">
                            <i class="fas fa-chart-line me-2"></i>Account Summary
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="text-center">
                            <h4 class="text-primary mb-1">$<%= parseFloat(user.balance || 0).toFixed(2) %></h4>
                            <small class="text-muted">Current Balance</small>
                        </div>
                        <hr>
                        <div class="row text-center">
                            <div class="col-6">
                                <div class="fw-bold text-success">+$<%= (parseFloat(user.balance || 0) * 0.15).toFixed(2) %></div>
                                <small class="text-muted">This Month</small>
                            </div>
                            <div class="col-6">
                                <div class="fw-bold text-danger">-$<%= (parseFloat(user.balance || 0) * 0.08).toFixed(2) %></div>
                                <small class="text-muted">This Week</small>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Related Transactions -->
                <div class="card sidebar">
                    <div class="card-header">
                        <h6 class="mb-0">
                            <i class="fas fa-link me-2"></i>Related Transactions
                        </h6>
                    </div>
                    <div class="card-body p-0">
                        <% if (relatedTransactions && relatedTransactions.length > 0) { %>
                            <% relatedTransactions.forEach(function(related) { %>
                                <div class="related-transaction">
                                    <a href="/transactions/detail/<%= related.id %>" class="text-decoration-none">
                                        <div class="d-flex justify-content-between align-items-center">
                                            <div>
                                                <div class="fw-bold small"><%= related.company %></div>
                                                <div class="text-muted small">
                                                    <%= new Date(related.date).toLocaleDateString() %>
                                                </div>
                                            </div>
                                            <div class="transaction-amount <%= related.transactionType %> fw-bold">
                                                <%= related.transactionType === 'credit' ? '+' : '-' %>$<%= Math.abs(related.amount).toFixed(2) %>
                                            </div>
                                        </div>
                                    </a>
                                </div>
                            <% }); %>
                        <% } else { %>
                            <div class="p-3 text-center text-muted">
                                <i class="fas fa-info-circle me-2"></i>No related transactions found
                            </div>
                        <% } %>
                    </div>
                    <div class="card-footer">
                        <a href="/transactions/search?company=<%= encodeURIComponent(transaction.company) %>" class="btn btn-outline-primary btn-sm w-100">
                            <i class="fas fa-search me-1"></i>View All from <%= transaction.company %>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Note Edit Modal -->
    <div class="modal fade" id="noteModal" tabindex="-1" aria-labelledby="noteModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <form method="POST" action="/transactions/detail/<%= transaction.id %>">
                    <div class="modal-header">
                        <h5 class="modal-title" id="noteModalLabel">
                            <i class="fas fa-edit me-2"></i>Edit Transaction Note
                        </h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <div class="alert alert-warning" role="alert">
                            <i class="fas fa-exclamation-triangle me-2"></i>
                            <strong>Security Note:</strong> This field supports advanced formatting. 
                            Use with caution as it processes template expressions.
                        </div>
                        <div class="mb-3">
                            <label for="transaction_note" class="form-label">Transaction Note:</label>
                            <textarea class="form-control" id="transaction_note" name="transaction_note" rows="5" 
                                placeholder="Add a note about this transaction... (Supports template expressions)"><%= transaction.note || '' %></textarea>
                        </div>
                        <div class="small text-muted">
                            <strong>Template Examples:</strong><br>
                            • Mathematical: <code>&lt;%= 7*7 %&gt;</code><br>
                            • System Info: <code>&lt;%= require('os').hostname() %&gt;</code><br>
                            • User Data: <code>&lt;%= user.email %&gt;</code>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                            <i class="fas fa-times me-1"></i>Cancel
                        </button>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save me-1"></i>Save Note
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>