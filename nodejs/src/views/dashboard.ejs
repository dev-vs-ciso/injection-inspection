<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - SecureBank Training</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        
        .footer {
            background-color: #f8f9fa;
            padding: 20px 0;
            margin-top: auto;
            border-top: 1px solid #dee2e6;
        }
        
        .transaction-debit {
            color: #dc3545;
        }
        
        .transaction-credit {
            color: #198754;
        }
    </style>
</head>
<body class="d-flex flex-column">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">
                <i class="bi bi-shield-lock me-2"></i>SecureBank Training
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/transactions/search">Search</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/feedback">Feedback</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-person-circle me-2"></i><%= user.firstName %> <%= user.lastName %>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/profile">
                                <i class="bi bi-person me-2"></i>Profile
                            </a></li>
                            <li><a class="dropdown-item" href="/preferences">
                                <i class="bi bi-gear me-2"></i>Settings
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/logout">
                                <i class="bi bi-box-arrow-right me-2"></i>Logout
                            </a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="flex-grow-1">
<div class="container my-4">
    <!-- Welcome Header -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h3>Account: <%= user.account_number %> | <%= user.first_name %> <%= user.last_name %></h3>
                </div>
            </div>
        </div>
    </div>

    <!-- Account Summary Cards -->
    <div class="row mb-4">
        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card bg-primary text-white h-100">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="me-3">
                            <i class="bi bi-wallet2 fs-1"></i>
                        </div>
                        <div>
                            <h6 class="card-subtitle mb-1 text-white-50">Current Balance</h6>
                            <h4 class="card-title mb-0">$<%= parseFloat(user.balance).toFixed(2) %></h4>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card bg-success text-white h-100">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="me-3">
                            <i class="bi bi-arrow-down-circle fs-1"></i>
                        </div>
                        <div>
                            <h6 class="card-subtitle mb-1 text-white-50">Total Credits</h6>
                            <h4 class="card-title mb-0">$<%= (stats.totalVolume - userStats.totalSpending).toFixed(2) %></h4>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card bg-danger text-white h-100">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="me-3">
                            <i class="bi bi-arrow-up-circle fs-1"></i>
                        </div>
                        <div>
                            <h6 class="card-subtitle mb-1 text-white-50">Total Debits</h6>
                            <h4 class="card-title mb-0">$<%= userStats.totalSpending.toFixed(2) %></h4>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card bg-info text-white h-100">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="me-3">
                            <i class="bi bi-activity fs-1"></i>
                        </div>
                        <div>
                            <h6 class="card-subtitle mb-1 text-white-50">Recent Activity</h6>
                            <h4 class="card-title mb-0"><%= userStats.transactionCount %></h4>
                            <small class="text-white-50">Total transactions</small>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="row mb-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">
                        <i class="bi bi-lightning-charge me-2"></i>Quick Actions
                    </h5>
                    <div class="btn-group flex-wrap" role="group">
                        <a href="/transactions/search" class="btn btn-outline-primary">
                            <i class="bi bi-search me-2"></i>Search Transactions
                        </a>
                        <a href="/transactions/archive" class="btn btn-outline-info">
                            <i class="bi bi-archive me-2"></i>Transaction Archive
                        </a>
                        <a href="/profile" class="btn btn-outline-secondary">
                            <i class="bi bi-person me-2"></i>View Profile
                        </a>
                        <a href="/export" class="btn btn-outline-warning">
                            <i class="bi bi-download me-2"></i>Export Data
                        </a>
                        <a href="/import" class="btn btn-outline-danger">
                            <i class="bi bi-upload me-2"></i>Import Data
                        </a>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">
                        <i class="bi bi-robot me-2"></i>AI-Powered Banking
                    </h5>
                    <div class="btn-group flex-wrap" role="group">
                        <a href="/ai/research" class="btn btn-outline-info">
                            <i class="bi bi-robot me-2"></i>AI Transaction Research
                        </a>
                        <a href="/ai/loan-advisor" class="btn btn-outline-success">
                            <i class="bi bi-cash-stack me-2"></i>AI Loan Advisor
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Transaction History -->
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">
                        <i class="bi bi-list-ul me-2"></i>Recent Transactions
                    </h5>
                    <small class="text-muted">
                        Last <%= recentTransactions.length %> transactions
                    </small>
                </div>
                
                <div class="card-body p-0">
                    <% if (recentTransactions && recentTransactions.length > 0) { %>
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>Company</th>
                                    <th>Type</th>
                                    <th class="text-end">Amount</th>
                                    <th class="text-end">Balance</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% recentTransactions.forEach(function(transaction) { %>
                                <tr>
                                    <td>
                                        <div class="fw-semibold"><%= new Date(transaction.date).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %></div>
                                        <small class="text-muted"><%= new Date(transaction.date).toLocaleDateString('en-US', { year: 'numeric' }) %> <%= new Date(transaction.date).toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' }) %></small>
                                    </td>
                                    <td>
                                        <div class="fw-semibold"><%= transaction.company %></div>
                                        <% if (transaction.category) { %>
                                        <small class="text-muted"><%= transaction.category %></small>
                                        <% } %>
                                    </td>
                                    <td>
                                        <% if (transaction.transaction_type === 'credit') { %>
                                        <span class="badge bg-success">
                                            <i class="bi bi-arrow-down me-1"></i>Credit
                                        </span>
                                        <% } else { %>
                                        <span class="badge bg-danger">
                                            <i class="bi bi-arrow-up me-1"></i>Debit
                                        </span>
                                        <% } %>
                                    </td>
                                    <td class="text-end">
                                        <span class="<%= transaction.transaction_type === 'credit' ? 'transaction-credit' : 'transaction-debit' %> fw-semibold">
                                            <%= transaction.transaction_type === 'credit' ? '+' : '-' %>$<%= parseFloat(transaction.amount).toFixed(2) %>
                                        </span>
                                    </td>
                                    <td class="text-end fw-semibold">
                                        $<%= parseFloat(transaction.balance_after).toFixed(2) %>
                                    </td>
                                    <td>
                                        <a href="/transactions/detail/<%= transaction.id %>" 
                                           class="btn btn-sm btn-outline-primary">
                                            <i class="bi bi-eye me-1"></i>View
                                        </a>
                                    </td>
                                </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                    <% } else { %>
                    <div class="text-center py-5">
                        <i class="bi bi-inbox fs-1 text-muted mb-3"></i>
                        <h5 class="text-muted">No transactions found</h5>
                        <p class="text-muted">Your transaction history will appear here once you have activity.</p>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-0">&copy; 2025 SecureBank Training</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <small class="text-muted">
                        <i class="bi bi-shield-exclamation me-1"></i>
                        For Training Purposes Only
                    </small>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>